# Basic Context Fixture

Use this short fixture to verify that `ctxshrink estimate` can read files from
a checkout and report deterministic estimate fields.

## Notes

- Keep examples small enough for quick release smoke tests.
- Prefer non-sensitive text so command output can be copied into PR evidence.
